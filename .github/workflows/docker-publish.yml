name: Docker Image CI

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.srikanthberla }}
          password: ${{ secrets.dckr_pat_7RfCmseVwSIBAFxmaMWJ6oBfm8I }}

      - name: Build Docker image
        run: docker build -t srikanthberla/my-app:latest .

      - name: Push Docker image
        run: docker push srikanthberla/my-app:latest
